fixtures:
  chart_pivot.xlsx:
    description: "Has charts and pivot tables, triggering tier A"
    inventory:
      has_charts: true
      has_pivot_tables: true
      has_pivot_caches: true
      requires_tier_a: true
  macro.xlsm:
    description: "Has VBA macro, triggering tier A"
    inventory:
      has_macros: true
      requires_tier_a: true
  simple.xlsx:
    description: "Simple file, safe for tier B"
    inventory:
      requires_tier_a: false
  r08_outlier.xlsx:
    description: "File with formula outlier for R08"
    inventory:
      requires_tier_a: false
  r09_number_as_text.xlsx:
    description: "File with numbers stored as text for R09"
    inventory:
      requires_tier_a: false
  r10_date_as_text.xlsx:
    description: "File with dates stored as text for R10"
    inventory:
      requires_tier_a: false

